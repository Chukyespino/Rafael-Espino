import React from "react";
import Layout from "../components/layout";

export default () => (
<Layout>
    <div>
        <h2>GitLens - Git, Supercharged</h2>
        <p>
        GitLens is a super tool if you are working on a project that involves multiple team members.
        It lets you peek into the last revision of the code precisely from the line where your cursor is.
        </p>
    </div>
</Layout>
);
